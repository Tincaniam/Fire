export default function RavenIcon({ className }: { className?: string }) {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/ravendock_nobg.svg"
      alt=""
      aria-hidden="true"
      className={className}
    />
  );
}
